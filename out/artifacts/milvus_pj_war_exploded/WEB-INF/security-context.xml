<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/security  http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id = "loginSuccess" class ="security.LoginSuccessHandler"/>
    <bean id ="bcryptPasswordEncoder" class ="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <security:http auto-config="true" use-expressions="true">
        <security:intercept-url pattern="/user/login" access="permitAll()"/>

        <security:intercept-url pattern="/user/list" access="hasRole('ROLE_ADMIN')"/>
        
        <security:intercept-url pattern="/discussion/list" access="hasRole('ROLE_MEMBER')"/>

        <security:form-login login-page="/user/adminLogin"
            authentication-success-handler-ref="loginSuccess"/>
        <security:logout logout-url="/user/log-out" invalidate-session="true"/>

        <security:access-denied-handler error-page="/accessError"/>

        <security:csrf disabled="true"/>
        <!-- csrf 토큰을 보내게 되면 view 단에서 처리해 줘야 하기 떄문에 비 활성화 한다 -->
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider>   <!--user-service-ref="userService"-->
            <!--<security:user-service>
                <security:user name="member" password="{noop}member" authorities ="ROLE_MEMBER"/>
                <security:user name="admin" password="{noop}admin" authorities ="ROLE_ADMIN,ROLE_MEMBER"/>
            </security:user-service>-->

           <security:jdbc-user-service
                   data-source-ref="dataSource"
                users-by-username-query = "SELECT userId,pw,enabled from tbl_user where userId =? "
                authorities-by-username-query="SELECT userId,auth from tbl_authorities where userId =?"
           />

           <security:password-encoder ref ="bcryptPasswordEncoder"/>

        </security:authentication-provider>
    </security:authentication-manager>
</beans>